
model Pokemon {
  @@map("pokemon")

  id        Int        @id @default(autoincrement())
  order     Int
  name      String
  image     String?
  weight    Int?
  createdAt DateTime   @default(now())
  updatedAt DateTime?  @updatedAt
}
